#include<stdio.h>

int main()
{
    //4th problem solve the below
    int a,b,sum,sub,mul;
    printf("Enter two number: ");
    scanf("%d%d",&a,&b);
    sum=a+b;
    sub=a-b;
    mul=a*b;
    printf("The sum is %d\n",sum);
    printf("The subtraction is %d\n",sub);
    printf("The multiple is %d",mul);
    return 0;
}

